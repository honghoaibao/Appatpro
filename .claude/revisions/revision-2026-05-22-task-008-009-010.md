# Revision: 2026-05-22 (session 4) — TASK-008, 009, 010 + ADR-0003

## TASK-008 — Integration tests

**Files**: `src/test/.../AutomationEngineIntegrationTest.kt`

6 tests covering farm loop behavior:
| Test | Scenario |
|------|----------|
| A | launchTikTok fails → checkpoint + empty session |
| B | Feed never loads → switch fails → checkpoint + empty session |
| C | Switch success + minutesPerAccount=0 → clean close, no checkpoint |
| D | Switch success + detectCheckpoint=true mid-loop → break + checkpoint |
| E | 3 accounts → all processed in order (coVerifyOrder) |
| F | parseAccountList returns accounts → addAccount for new ones only |

Technique: `mockkObject(NodeTraverser)` + `answers {}` lambda for `findByText` routing by text content.

**Rollback**: Delete file. No behavior change.

---

## TASK-009 — AccountsScreen

**Files**: `ui/accounts/AccountsViewModel.kt`, `AccountsScreen.kt`, `AccountsActivity.kt`
**Also**: `LocalRepository.kt` — added `updateStatus(username, status)` delegating to `AccountDao.updateStatus`

Features:
- `AccountDao.observeAll()` → `StateFlow<List<AccountEntity>>` via `stateIn(WhileSubscribed)`
- Search field (text filter on username)
- 4 filter chips: Tất cả / Hoạt động / Checkpoint / Bị khóa — counts live from accounts list
- `SwipeToDismissBox` (end-to-start) → confirmation `AlertDialog` → `vm.delete()`
- Status dot + badge color: Green/Amber/Red per status
- Mini stats per row: sessions, likes, follows
- Empty state with auto-discover hint
- `FeatureFlags.NATIVE_ACCOUNTS = false` (off by default)

**Rollback**: `FeatureFlags.NATIVE_ACCOUNTS = false` (already default).

---

## TASK-010 — LogsScreen

**Files**: `ui/logs/LogsViewModel.kt`, `LogsScreen.kt`, `LogsActivity.kt`

Features:
- `LogDao.observeRecent(500)` → combined with `_levelFilter` StateFlow → `StateFlow<List<FarmLogEntity>>`
- Level filter chips: Tất cả / Info / OK / Cảnh báo / Lỗi
- Auto-scroll to top on new log (`LaunchedEffect` on `logs.first().id`)
- Copy-all to clipboard (formatted `[HH:mm:ss] message`)
- Clear-all via `logDao.clearAll()`
- Accent bar per row colored by level (blue/green/amber/red)
- Monospace font for log message text
- `FeatureFlags.NATIVE_LOGS = false` (off by default)

**Rollback**: `FeatureFlags.NATIVE_LOGS = false` (already default).

---

## ShortcutRow navigation wired

`DashboardScreen.kt` ShortcutRow callbacks now:
- Tài khoản → `AccountsActivity` if `NATIVE_ACCOUNTS` else `FlutterBridge.sendEvent("navigate", /accounts)`
- Thống kê → always Flutter (`/stats` not yet migrated)
- Nhật ký → `LogsActivity` if `NATIVE_LOGS` else `FlutterBridge.sendEvent("navigate", /logs)`

---

## AndroidManifest.xml

Added:
- `.ui.accounts.AccountsActivity` (exported=false)
- `.ui.logs.LogsActivity` (exported=false)

---

## FeatureFlags.kt

Updated: 3 flags (`NATIVE_DASHBOARD`, `NATIVE_ACCOUNTS`, `NATIVE_LOGS`), all false.

---

## ADR-0003

Created: `.claude/decisions/ADR-0003-native-compose-migration.md`
Documents: screen-by-screen migration strategy, flag rules, order, rollback, review trigger.

---

## Files changed

| File | Change |
|------|--------|
| `test/.../AutomationEngineIntegrationTest.kt` | Created (6 tests) |
| `ui/accounts/AccountsViewModel.kt` | Created |
| `ui/accounts/AccountsScreen.kt` | Created |
| `ui/accounts/AccountsActivity.kt` | Created |
| `ui/logs/LogsViewModel.kt` | Created |
| `ui/logs/LogsScreen.kt` | Created |
| `ui/logs/LogsActivity.kt` | Created |
| `ui/FeatureFlags.kt` | Updated (3 flags) |
| `ui/dashboard/DashboardScreen.kt` | ShortcutRow navigation wired |
| `data/LocalRepository.kt` | updateStatus() added |
| `AndroidManifest.xml` | 2 new activities registered |
| `bridge/FlutterBridge.kt` | getFeatureFlags updated (3 flags) |
| `.claude/decisions/ADR-0003-*.md` | Created |
