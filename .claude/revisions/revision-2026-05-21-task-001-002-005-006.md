# Revision: 2026-05-21 — TASK-001, 002, 005, 006

## What changed

### TASK-001: Entities split
- Deleted: `db/entity/Entities.kt` (monolith)
- Created: `db/entity/AccountEntity.kt`, `SessionEntity.kt`, `FarmLogEntity.kt`, `ConfigEntity.kt`
- `DailyStatRow` co-located in `SessionEntity.kt` (return type của SessionDao, không có bảng riêng)
- Mỗi file có KDoc giải thích entity purpose + field semantics

### TASK-002: DAOs split
- Deleted: `db/dao/Daos.kt` (monolith)
- Created: `db/dao/AccountDao.kt`, `SessionDao.kt`, `LogDao.kt`, `ConfigDao.kt`
- `TotalsRow` co-located trong `SessionDao.kt`
- `AtProDatabase.kt` và `LocalRepository.kt` không cần đổi import (wildcard `*` vẫn đúng)

### TASK-005: LanWebSocketServer network reconnect
- `start()` → `start(context: Context)` — nhận Context để dùng ConnectivityManager
- Đăng ký `ConnectivityManager.NetworkCallback` trong `start()`, unregister trong `stop()`
- `onAvailable` → `scheduleRestart(2000ms)` (debounced — cancel + relaunch)
- `onLost` → `stopServer()` ngay lập tức
- `FarmForegroundService.onCreate()` cập nhật: `LanWebSocketServer.start(applicationContext)`
- Thêm `serverScope` module-level cho reconnect coroutines
- Server restart sequence: `stopServer()` → `delay(500ms)` → `startServer()`

### TASK-006: PopupHandler documentation
- Mọi `when` branch trong `handleKnownPopup()` có comment: popup type + TikTok scenario
- `handle1234()` có comment step-by-step + lý do delay
- `scanKeywords()` có comment từng keyword group (xác minh, thông báo, cập nhật, GDPR...)
- `handleByKeyword()` có comment thứ tự ưu tiên dismiss texts
- `getAllText()` có comment lý do dùng BFS thay DFS

## Files changed

| File | Change |
|------|--------|
| `db/entity/Entities.kt` | Deleted |
| `db/entity/AccountEntity.kt` | Created |
| `db/entity/SessionEntity.kt` | Created (+ DailyStatRow) |
| `db/entity/FarmLogEntity.kt` | Created |
| `db/entity/ConfigEntity.kt` | Created |
| `db/dao/Daos.kt` | Deleted |
| `db/dao/AccountDao.kt` | Created |
| `db/dao/SessionDao.kt` | Created (+ TotalsRow) |
| `db/dao/LogDao.kt` | Created |
| `db/dao/ConfigDao.kt` | Created |
| `network/LanWebSocketServer.kt` | Modified — context param, NetworkCallback, serverScope |
| `data/FarmForegroundService.kt` | Modified — pass applicationContext to start() |
| `automation/popup/PopupHandler.kt` | Modified — full inline documentation |

## Rollback note

TASK-001 + TASK-002: Khôi phục `Entities.kt` và `Daos.kt` từ git, xóa 8 file mới. Package không đổi nên không có gì compile-break.

TASK-005: Đổi `start(context)` về `start()` trong `LanWebSocketServer`, xóa networkCallback/scheduleRestart logic, sửa `FarmForegroundService` về lại `LanWebSocketServer.start()`.

TASK-006: Xóa comments. Behavior không đổi, chỉ là docs.

## Risks / follow-ups

- `serverScope` là module-level — xem TD-006
- `NetworkRequest` chỉ filter WIFI + ETHERNET. Nếu muốn support USB tethering, thêm `TRANSPORT_USB`
- Keyword list trong `scanKeywords()` cần maintenance khi TikTok update wording (xem TD-003 cũ — đã resolved một phần, cần ongoing update)
- Remaining backlog: TASK-003 (test AutomationEngine) và TASK-004 (OEM battery audit) — HIGH priority

## ADR reference
ADR-0001, ADR-0002
